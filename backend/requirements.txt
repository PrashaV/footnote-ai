# Pinned Python dependencies for the Footnote FastAPI backend.
# Exact versions — no loose ranges. Regenerate with `pip freeze` after installs.

fastapi==0.115.6
uvicorn[standard]==0.32.1
anthropic==0.40.0
pydantic==2.10.3
python-dotenv==1.0.1
slowapi==0.1.9
httpx==0.28.1
python-docx==1.1.2
